<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.orange.mall.app">

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

  <application
    android:name=".Application"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    android:hardwareAccelerated="true">

    <activity
      android:name=".modules.login.LoginActivity"
      android:theme="@style/Theme.MyNoTitleBarTheme" />

    <activity
      android:name=".modules.splash.SplashActivity"
      android:theme="@style/Theme.MyNoTitleBarTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name=".modules.main.MainActivity"
      android:theme="@style/Theme.MyNoTitleBarTheme" />

    <activity android:name=".modules.register.RegisterActivity"
      android:theme="@style/Theme.MyNoTitleBarTheme"/>

    <activity android:name=".modules.forgetpassword.ForgetPasswordActivity"
      android:theme="@style/Theme.MyNoTitleBarTheme"></activity>


  </application>

</manifest>